# Migration
Preview first:
```bash
omnitech-rulesync migrate --from rulesync-bonsai <target>
omnitech-rulesync migrate --from omnitech-shared <target>
```
Predecessor repositories remain read-only. Resolve collisions with target-owned profile overrides.
