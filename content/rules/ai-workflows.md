---
targets: ["*"]
description: AI SDK, LangChain, and LangGraph reliability rules
---
# AI workflows
- Preserve streaming, structured output, cancellation, and provider boundaries.
- Make provider selection explicit and observable.
- Test malformed model output, parsers, retries, and resumability.
- Never silently fall back to an external provider from local-only configuration.
